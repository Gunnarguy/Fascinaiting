// JavaScript function to toggle the menu
function toggleMenu() {
  const menu = document.querySelector('.nav-menu');
  menu.classList.toggle('responsive');
}
// Remember to make sure this file is loaded after the HTML content is fully loaded
// or attached to an event that listens for when the DOM is ready.
